name: CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install zsh
        run: sudo apt-get install zsh -y --no-install-recommends

      - name: Install dotfiles
        run: ./install
